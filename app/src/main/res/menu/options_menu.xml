<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/search"
        android:title="@string/search_title"
        android:icon="@drawable/ic_search_black_24dp"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="collapseActionView|ifRoom" />

    <item android:id="@+id/display_all"
        android:title="@string/display_all_title" />

    <item android:id="@+id/with_title"
        android:title="@string/with_title_title" />

    <item android:id="@+id/with_description"
        android:title="@string/with_description_title" />

    <item android:id="@+id/logout"
        android:title="@string/logout_title" />
</menu>